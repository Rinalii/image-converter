# ImgLib — универсальный конвертер изображений
## Проект для портфолио | C++17 | CMake | libjpeg

ImgLib — это практическая реализация инструмента для чтения, записи и конвертации растровых изображений в форматах PPM, JPEG и BMP (24‑бит без сжатия).

Проект демонстрирует уверенное владение современным C++, умение интегрировать C‑библиотеки (libjpeg) и самостоятельно реализовывать бинарные форматы файлов в соответствии со спецификацией.

## ⚙️ Сборка и запуск
### Требования
- C++17
- CMake 3.10+

### Сборка
```
git clone https://github.com/yourusername/cpp-image-converter.git
cd cpp-image-converter
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .
```

## Запуск конвертера

```
./ImgLib <входной_файл> <выходной_файл>
```

Формат определяется исключительно по расширению файла (.ppm, .jpg, .jpeg, .bmp)

## 🧱 Архитектура и особенности реализации
### 📁 Структура проекта

```
cpp-image-converter/
├── ImgConverter/
|   ├── CMakeLists.txt
│   └── main.cpp
├── ImgLib/
|   ├── CMakeLists.txt
│   ├── bmp_image.h / .cpp         # Реализация работы с BMP
│   ├── img_lib.h / .cpp           # Общая логика обработки изображений         
│   ├── jpeg_image.h / .cpp        # Реализация работы с JPEG
│   ├── pack_defines.h    
│   ├── ppm_image.h / .cpp         # Реализация работы с PPM
└── README.md
```
### 🔹 Внутреннее представление — img_lib::Image

- Пиксель — Color (r, g, b, a) — RGB‑порядок, 1 байт на канал.
- Изображение хранится как одномерный std::vector<Color> с отступом между строками (step).
- Поддерживается произвольный доступ по координатам.

### 🔹 Интерфейс форматов — ImageFormatInterface

```
class ImageFormatInterface {
public:
    virtual bool SaveImage(const img_lib::Path& file, const img_lib::Image& image) const = 0;
    virtual img_lib::Image LoadImage(const img_lib::Path& file) const = 0;
};
```

### 🔹 Фабрика GetFormatInterface
Возвращает указатель на нужный интерфейс на основе расширения файла.

### 🔹 main.cpp — алгоритм конвертации
- Проверка аргументов командной строки.
- Получение интерфейса для входного и выходного файлов.
- Загрузка изображения (input_format->LoadImage).
- Сохранение (output_format->SaveImage).
- Обработка ошибок (коды возврата 0–5, перехват std::runtime_error).
